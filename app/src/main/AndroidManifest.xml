<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.news">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@drawable/newspaper"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NEWS">
        <activity android:name=".WellcomePage.SplashScreen">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".WellcomePage.WellcomeActivity">
        </activity>
        <activity android:name=".Trail.Connectivity" />
        <activity android:name=".RegistrationAndLogin.MyLoginActivity"></activity>
        <activity android:name=".RegistrationAndLogin.SignupActivity" />
        <activity android:name=".AppleActivity" />
        <activity android:name=".Web.WebActivtiy2" />
        <activity android:name=".Web.WebActivity" />
        <activity android:name=".BusinessNewsActivity" />
        <activity android:name=".TechCrunchActivity" />
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>